#admin-users(layout="row" flex)
  md-list.users(flex)
    md-list-item(ng-repeat="user in users" layout-wrap)
      .md-avatar
        md-icon.material-icons.md-icon-avatar(md-font-icon="icon-person-outline" ng-if="!isJudgeTwo(user)")
        md-icon.material-icons.md-icon-avatar(md-font-icon="icon-person" ng-if="isJudgeTwo(user)")
      .md-list-item-text
        .name
          span {{ user.username }}
          span(ng-if="user.lastname || user.firstname") -
          span {{ user.lastname | uppercase }} {{ user.firstname }}
        .email {{ user.email }}
      div(flex)
      md-button.md-primary.md-raised(ng-click="addRole(user, 'judgeTwo')" ng-if="!isJudgeTwo(user)") Nommer arbitre
      md-button.md-primary.md-raised(ng-click="removeRole(user, 'judgeTwo')" ng-if="isJudgeTwo(user)") Retirer role arbitre


