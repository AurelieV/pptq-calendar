#login(layout="row" layout-align="center")
  md-content.md-whiteframe-z1(ng-if="!authenticationService.isAuthenticated()")
    h3 Connexion
    form(name="connectForm")
      md-input-container
        label Login
        input(type="text" required ng-model="credentials.username")
      md-input-container
        label Mot de passe
        input(type="password" required ng-model="credentials.password")
      .md-actions(layout="row" layout-align="space-around")
        md-button.md-primary(ng-click="authenticationService.connect(credentials)" ng-disabled="!connectForm.$valid") Se connecter
    div(layout="row" layout-align="center")
      a(ui-sref="create-login") Pas enregistré? Je crée un compte !

  md-content.md-whiteframe-z1(ng-if="authenticationService.isAuthenticated()")
    span.message Vous êtes déjà connecté en tant que
    span.login {{ authenticationService.getUser() }}
    .md-actions(layout="row" layout-align="space-around")
      md-button.md-primary(ng-click="authenticationService.disconnect()") Se déconnecter



